# 更新日志

## v1.1.3

### 变更
- 不依赖高度的图层生成速度大幅提升。这类图层改为「每列求值一次、整段高度复用」，不再逐格重算。实测这类图层的表达式求值次数减少约 50~90 倍。
- 方块字面量只解析一次，不再每次求值都查一遍注册表缓存——每个区块因此省去数万次查找。

### 兼容性
- 公式语法与行为均无变化，任何公式生成的地形都与之前完全一致。

## v1.1.2

### 修复
- 修复较新 NeoForge 版本拒绝加载本模组的问题。此前 `neoforge` 依赖被声明为精确版本（`[21.1.234]`）而非最低版本，导致所有高于 21.1.234 的加载器都报版本错误、用户只能回退加载器。
- 依赖现已声明为 `[21.1.234,)`，并改用 21.1.251 构建。

### 兼容性
- 可在 NeoForge 21.1.234 及之后所有 21.1.x 版本上加载。无需改动代码：本模组用到的 NeoForge 与 FancyModLoader 类在该区间内没有变化。

### 变更
- 移除了显式的 `bus = EventBusSubscriber.Bus.MOD` 参数。FancyModLoader 早已按订阅事件的类型自动路由（实现 `IModBusEvent` 走 mod 总线，其余走游戏总线），该参数实际不生效。它已被标记为待移除，现在去掉可确保将来正式移除时无需改动代码。

## v1.1.1

### 修复
- 修复 Fabric 客户端 Mixin 被专用服务器加载的问题。
- 修复 NeoForge 预设编辑器注册和连续创建世界时的状态问题。
- 修复嵌套 `let` 作用域求值，并拒绝返回非方块的层表达式。
- 修复公式异常留下部分区块以及基础高度计算错误的问题。

### 变更
- 公式快照现在绑定到具体的超平坦生成器。
- 增加高上限的输入长度和解析嵌套保护，不设置区块生成预算。

## v1.1.0

### 新增
- **加载已保存的公式**：公式编辑器中新增"加载公式"按钮，可从之前保存的 `.txt` 文件中选择并加载公式到编辑器。
- **实时错误反馈**：公式解析错误现在直接在编辑器中显示（所有按钮下方），包含行号和错误信息，不再静默失败。

### 修复
- **联机兼容性**：未安装本模组的玩家现在可以加入使用公式生成地形的世界。模组不再注册自定义区块生成器类型，所有逻辑通过 Mixin 注入原版超平坦生成器实现。
- **逻辑运算短路求值**：`&&` 和 `||` 运算符现在正确短路（左操作数已能确定结果时，不再求值右操作数）。
- **嵌套 let 块**：外层 `let` 块中声明的变量现在可以在嵌套的 `let` 块中引用。
- **循环层 `ly` 变量**：循环层中的 `ly` 现在正确表示层内相对 Y 坐标（与文档说明一致），而非每个条目的内部偏移。
- **未知函数/字符报错**：使用不支持的函数名或非法字符现在会产生明确的错误提示，而非静默生成全空气世界。

### 变更
- 从文档中移除了 `smooth` 和 `rng` 函数的说明（这两个函数从未实现）。
- 清理了调试日志。
- 规范了模组的 group ID 与实际包名一致。

---

## v1.0.3

- 首次公开发布。
